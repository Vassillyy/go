import type { ITopicConfig } from './types.ts';

export const sliceTopic: ITopicConfig = {
  sections: [
    {
      heading: 'Что такое слайс',
      body: 'Слайс — это дескриптор над массивом: указатель на первый доступный элемент, длина (len) и ёмкость (cap). Сам слайс данные не хранит, а лишь ссылается на часть какого-то массива в памяти — поэтому в отличие от массива слайс не является отдельным типом для каждой длины и может расти во время выполнения программы.',
    },
    {
      heading: 'Способы создания',
      body: 'Слайс можно создать литералом с элементами, через make с длиной и опционально ёмкостью, срезом уже существующего массива или слайса, либо просто объявить переменную — тогда получится nil-слайс без базового массива.',
      examples: [
        {
          caption: 'Литерал',
          code: 's := []int{1, 2, 3}\n' + 'fmt.Println(s)\n\n' + '// [1 2 3]',
        },
        {
          caption: 'make с длиной и ёмкостью',
          code:
            's := make([]int, 3, 5)\n' +
            'fmt.Println(s, len(s), cap(s))\n\n' +
            '// [0 0 0] 3 5',
        },
        {
          caption: 'Срез существующего массива',
          code:
            'a := [5]int{10, 20, 30, 40, 50}\n' +
            's := a[1:3]\n' +
            'fmt.Println(s)\n\n' +
            '// [20 30]',
        },
        {
          caption: 'var — nil-слайс',
          code:
            'var s []int\n' +
            'fmt.Println(s, s == nil, len(s))\n\n' +
            '// [] true 0',
        },
      ],
    },
    {
      heading: 'Длина и ёмкость',
      body: 'len(s) — число элементов, видимых в слайсе сейчас. cap(s) — сколько элементов помещается в него, начиная с его первого элемента и до конца базового массива, а не только тех, что видны. Поэтому у среза существующего слайса cap может быть заметно больше len.',
      examples: [
        {
          caption: 'cap считается до конца базового массива, а не до high',
          code:
            's := make([]int, 5, 10)\n' +
            't := s[2:4]\n' +
            'fmt.Println(len(t), cap(t))\n\n' +
            '// 2 8',
        },
      ],
    },
    {
      heading: 'append и реаллокация',
      body: 'append добавляет элементы в конец слайса. Если в базовом массиве есть свободное место по cap, append пишет прямо в него и возвращает слайс с тем же базовым массивом — увеличивается только len. Если места не хватает, append выделяет новый, более крупный массив, копирует туда все элементы и возвращает слайс уже поверх него — связь со старым массивом теряется.',
      examples: [
        {
          caption: 'Пока хватает cap — тот же базовый массив',
          code:
            's := make([]int, 2, 5)\n' +
            'p1 := &s[0]\n' +
            's = append(s, 3)\n' +
            'p2 := &s[0]\n' +
            'fmt.Println(p1 == p2)\n\n' +
            '// true',
        },
        {
          caption: 'При нехватке cap — новый базовый массив',
          code:
            's := make([]int, 2, 2)\n' +
            'p1 := &s[0]\n' +
            's = append(s, 3)\n' +
            'p2 := &s[0]\n' +
            'fmt.Println(p1 == p2)\n\n' +
            '// false',
        },
      ],
    },
    {
      heading: 'Общий базовый массив',
      body: 'Два слайса, полученные срезом одного и того же массива или слайса, могут указывать на общие данные. Изменение элемента через один слайс видно и через другой, пока оба остаются в пределах общей части базового массива. append, если он не реаллоцирует, тоже пишет в общий массив и может незаметно перезаписать данные, которые видит другой слайс.',
      examples: [
        {
          caption: 'Изменение через один слайс видно в другом',
          code:
            'a := [5]int{1, 2, 3, 4, 5}\n' +
            's1 := a[0:3]\n' +
            's2 := a[1:4]\n' +
            's1[1] = 999\n' +
            'fmt.Println(s2)\n\n' +
            '// [999 4 5]',
        },
      ],
    },
    {
      heading: 'copy — независимая копия',
      body: 'Чтобы получить слайс, полностью независимый от оригинала, данные копируют явно функцией copy в заранее созданный слайс нужной длины — присваивание или обычный срез копию данных не делают, они лишь создают ещё один заголовок над теми же данными.',
      examples: [
        {
          code:
            'src := []int{1, 2, 3}\n' +
            'dst := make([]int, len(src))\n' +
            'copy(dst, src)\n' +
            'dst[0] = 999\n' +
            'fmt.Println(src, dst)\n\n' +
            '// [1 2 3] [999 2 3]',
        },
      ],
    },
    {
      heading: 'Трёхиндексный срез',
      body: 'Полная форма среза a[low:high:max] задаёт не только границы видимой части (len = high-low), но и предел ёмкости результата (cap = max-low). Это защищает от случая, когда append, не реаллоцируя, затирает данные за пределами high, которые всё ещё принадлежат исходному массиву и используются где-то ещё.',
      examples: [
        {
          code:
            'a := [5]int{1, 2, 3, 4, 5}\n' +
            's := a[1:3:3]\n' +
            'fmt.Println(len(s), cap(s))\n\n' +
            '// 2 2',
        },
      ],
    },
    {
      heading: 'nil и пустой слайс',
      body: 'var s []int создаёт nil-слайс: у него нет базового массива, s == nil истинно, а len и cap равны нулю — но в него всё равно можно добавлять через append, новый массив он выделит сам. []int{} — это уже не nil, а пустой слайс с базовым массивом нулевого размера: s == nil для него ложно, хотя по длине и содержимому он неотличим от nil-слайса.',
      examples: [
        {
          code:
            'var a []int\n' +
            'b := []int{}\n' +
            'fmt.Println(a == nil, b == nil)\n\n' +
            '// true false',
        },
      ],
    },
    {
      heading: 'Сравнение',
      body: 'Слайс нельзя сравнить оператором == с другим слайсом — это ошибка компиляции; единственное разрешённое сравнение слайса — с nil. По той же причине слайс нельзя использовать как ключ map.',
      examples: [
        {
          code:
            's1 := []int{1, 2}\n' +
            's2 := []int{1, 2}\n' +
            'fmt.Println(s1 == s2)\n\n' +
            '// invalid operation: s1 == s2 (slice can only be compared to nil)',
        },
      ],
    },
    {
      heading: 'Перебор элементов',
      body: 'range по слайсу на каждой итерации отдаёт индекс и копию значения элемента. В отличие от массива, сам слайс перед циклом не копируется целиком — копируется только его небольшой заголовок, поэтому range читает данные из общего базового массива по ходу цикла и видит изменения, сделанные через индексацию s[i] на предыдущих итерациях.',
      examples: [
        {
          code:
            's := []int{1, 2, 3}\n' +
            'for i, v := range s {\n' +
            '  if i == 0 {\n' +
            '    s[1] = 999\n' +
            '  }\n' +
            '  fmt.Println(i, v)\n' +
            '}\n\n' +
            '// 0 1\n' +
            '// 1 999\n' +
            '// 2 3',
        },
      ],
    },
  ],
};